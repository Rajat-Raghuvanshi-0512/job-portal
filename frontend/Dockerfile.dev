# Development Dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Expose port for Angular server
EXPOSE 4200
# Run dev server - binding to 0.0.0.0 is crucial in Docker
CMD ["npm", "start", "--", "--host", "0.0.0.0"]
